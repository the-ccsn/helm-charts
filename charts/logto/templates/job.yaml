{{- if include "logto.installPreAppJob" . }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "logto.preAppJobName" . }}
spec:
  ttlSecondsAfterFinished: 86000 # Clean up the job after 1 day.
  template:
    spec:
      restartPolicy: Never # The database alteration job should never be restarted on failure, as it may cause data corruption. Handle retries manually if needed.
      initContainers:
        - name: wait-for-db
          {{- include "logto.waitForDbContainerSpec" . | nindent 10 }}
        {{- if .Values.autoSeeding.enabled }}
        - name: {{ .Chart.Name }}-seed
          {{- include "logto.commonContainerSpec" (dict "dbUrl" (.Values.autoSeeding.dbUrl | default nil) "embeddedTls" (dict "enabled" false) "volumeMounts" (list) "context" .) | nindent 10 }}
          command:
            - /bin/sh
          args:
            - '-c'
            - 'npm run cli db seed -- --swe'
        {{- end }}
        {{- if .Values.autoAlteration.enabled }}
        - name: {{ .Chart.Name }}-alteration
          {{- include "logto.commonContainerSpec" (dict "dbUrl" (.Values.autoAlteration.dbUrl | default nil) "embeddedTls" (dict "enabled" false) "volumeMounts" (list) "context" .) | nindent 10 }}
          command:
            - /bin/sh
          args:
            - '-c'
            - 'npm run alteration deploy latest'
        {{- end }}
      containers:
      - name: pre-app-complete
        image: busybox:latest
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
          - /bin/sh
          - -c
          - 'echo "Pre-app initialization complete."'

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  backoffLimit: 0 # Disable automatic retries to prevent potential data corruption. Handle retries manually if needed.
{{- end -}}
